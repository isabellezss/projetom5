export class Eventos {
    constructor(name, data, local, descricao) {
        this.name = name;
        this.data= data;
        this.local = local;
        this.descricao = descricao;
    }
}